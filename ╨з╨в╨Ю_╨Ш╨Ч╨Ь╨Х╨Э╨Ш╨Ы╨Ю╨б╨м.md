# Что изменилось в этой версии

## Форма приведена к виду 1С «Калькуляция»
- Убрано поле «Клиент».
- Склад, Тип перевозки, Вид товара, Тип упаковки — выпадающие списки
  (значения тянутся из БД; если пусто — из дефолтов в начале app.py).
- Добавлены: количество мест / в упаковке, галки «Бренд» и «Z-товар».

## Ветка «Расчёт по новому» — реализована (была заглушкой)
Логика перенесена побайтово из общего модуля `тп_РасчетЦен_Служебная`
(процедура `РасчетСреднейЦеныПоНовому`), исходник — в `02_ИзвлечённыеИсходники/`.
Формула: перевозка (тп_ТарифыНаПеревозку) + растаможка (тп_ЦеныНаРастаможку)
+ наценки (бренд/Z). Файл: `tp_calc/extensions.py`.

Правила:
- Растаможка: по весу → вес/1000×цена; по количеству → места×в_упаковке×цена; иначе объём×цена.
- Перевозка: если ЛогистикаВключена → 0; иначе по весу → вес/1000×цена, по объёму → объём×цена.
  Тариф ищется по: тип перевозки + город склада + средний вес в диапазоне.
- Наценки: Бренд (объём×наценка, или вес×наценка если авиа), Z (объём×наценка).

## Защита паролем
Включается переменной окружения `APP_PASSWORD` (в Railway → Variables).
- `APP_PASSWORD` — пароль (если не задан, сайт открыт).
- `APP_USERNAME` — логин (по умолчанию `admin`).
Браузер спросит логин/пароль при входе.

## Что осталось вашим (extensions.py)
- `стоимость_последней_мили()` — last-mile, по умолчанию 0.
- Свои тарифы поверх стандартных — там же.
