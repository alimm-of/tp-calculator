# Как обновить цены на сайте

База данных лежит в репозитории готовым файлом `prices.db` (~4.6 МБ).
Приложение при старте использует его напрямую — быстро, без пересборки.

## Обновление цен (когда цены в 1С поменялись)

1. Выгрузите свежий `ЦеныТП_ГГГГММДД.json` из базы ТП (обработка ВыгрузкаЦен_JSON).
2. Локально пересоберите базу:
   ```
   python cli.py import ЦеныТП_ГГГГММДД.json prices.db
   ```
3. Закоммитьте обновлённый `prices.db` на GitHub → Railway пересоберётся.

## Почему так, а не JSON в репозитории

JSON-выгрузка весит ~30 МБ, готовая SQLite-база — ~4.6 МБ (в 6.5 раз меньше).
Railway не тратит время на пересборку базы при каждом запуске.
