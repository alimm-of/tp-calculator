# Как загрузить реальные цены из 1С в калькулятор

Это решает сразу три вещи: появятся склады в списке, заполнятся цены,
и расчёт перестанет показывать 0.

## Схема

```
База ТП ──[обработка ВыгрузкаЦен_JSON]──> цены.json ──[импорт]──> prices.db ──> сайт
```

## Шаг 1. Собрать внешнюю обработку в 1С (один раз)

Файл модуля: `ВыгрузкаЦен_JSON_МодульФормы.bsl` (лежит в проекте).

1. Конфигуратор 1С → Файл → Новый → **Внешняя обработка**.
2. Имя: `ВыгрузкаЦен_JSON`.
3. Добавить **реквизит формы** `ДатаСреза` (тип Дата) — на какую дату брать «срез последних» цен.
4. Создать управляемую форму, добавить команду «Выгрузить цены» с кнопкой.
5. Открыть модуль формы, вставить текст из `ВыгрузкаЦен_JSON_МодульФормы.bsl`.
6. Сохранить как `.epf`.

Обработка выгружает из базы (методом «срез последних» на ДатаСреза):
- цены по клиентам (по видам и группам товаров),
- **цены по видам товара** (это документ «Установка фиксированных цен по весу»),
- коэффициенты дорожных расходов, цену доп.расходов,
- **справочник складов** (с городом и признаком «только свои цены»),
- тарифы на перевозку, цены на растаможку, наценки, ставки за пресс.

## Шаг 2. Выгрузить файл

1. Открыть базу ТП в режиме 1С:Предприятие.
2. Файл → Открыть → выбрать `ВыгрузкаЦен_JSON.epf`.
3. Указать `ДатаСреза` (обычно сегодня).
4. Нажать «Выгрузить цены» → сохранить `цены.json`.

## Шаг 3. Загрузить в калькулятор

**Вариант А — через сайт (обновление на Railway):**
1. Переименуйте `цены.json` в `prices_sample.json`.
2. Положите его в папку `sample/` вашего репозитория (замените старый).
3. Закоммитьте на GitHub → Railway пересоберётся, база создастся из ваших цен.

**Вариант Б — локально (проверить перед заливкой):**
```bash
python cli.py import цены.json prices.db
python app.py            # http://127.0.0.1:5000 — проверить расчёт
```

## Проверка

После загрузки:
- в списке «Склад» появятся Bishkek, Dordoi, Foshan, Pekin, Keqiao и т.д.;
- расчёт по реальному виду товара + упаковке + складу даст ненулевую сумму;
- если по конкретной па57ре цены нет — покажется примечание «Базовая цена не найдена»
  (это нормально: значит в 1С для этой комбинации цена не задана).

## Важно про соответствие идентификаторов

Вид товара и упаковка сопоставляются по **Коду** справочника 1С (как в выгрузке).
Список видов товара в форме взят из вашего файла `виды_товаров.xlsx` — коды совпадают.
Склад сопоставляется по внутреннему идентификатору из выгрузки.
